---
# tasks file for role_worker
- name: 'Añadir puertos firewalld'
  firewalld:
    port: "{{ item }}"
    permanent: yes
    state: enabled
  loop:
    - 8285/udp
    - 8472/udp
    - 30000-32767/tcp
    - 10250/tcp
    - 27017/tcp
    - 32000/tcp
  notify: Reinicio firewall
  tags: worker
