# 1. Playbook principal: “playbook.yaml”. 
ansible-playbook -i inventory playbook.yaml 

# 2. Playbook join: “playbook_join.yaml”. 
ansible-playbook -i inventory playbook_join.yaml 

# 3. Playbook app: “playbook_app.yaml”. 
ansible-playbook -i inventory playbook_app.yaml 